# 2026-03-19

- 里程碑：`CanvasTerminalKit` 接入 `libghostty-spm`（`GhosttyTerminal`）。
- 决策：简单终端方案采用 `exec` 后端，避免先期自定义会话协议，先保证最小可用。
- 交付：新增 `SimpleTerminal` / `SimpleTerminalView` 与 `CanvasTerminalDemo` 可执行示例。
- 验证：`swift test` 与 `swift build` 均通过。
- 决策：Demo 启动改为 `.app bundle` 模式（`make run-demo-app`），不再用 `swift run` 作为主启动路径。
- 证据：系统日志出现过 `missing main bundle identifier`，并伴随窗口/输入异常；改为 app bundle 后可观测到窗口数为 1。
- 决策：示例运行从包内 executable 迁移到外置 Xcode Example 工程（`Example/CanvasTerminalExample.xcodeproj`）。
- 结果：`Package.swift` 仅保留库产物，`xcodebuild -list` 已识别 Example target/scheme。
