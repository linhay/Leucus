# 2026-03-21

- 版本：将 `Leucus` 示例工程 `MARKETING_VERSION` 从 `1.0` 调整为 `0.0.1`，与首发版本号对齐。
- 构建：执行 Release 构建成功，产物位于 `DerivedData/.../Build/Products/Release/Leucus.app`。
- 发布资产：生成 `Leucus-0.0.1.zip`（包含 `.app`）与 `appcast.xml`（指向 `v0.0.1` Release 下载地址）。
- 发布目标：创建 GitHub tag/release `v0.0.1`，触发自动同步工作流将 `appcast.xml` 与 `.zip` 分发到 `gh-pages`。
- 修复：将自动同步工作流重命名为 `release-assets-to-pages.yml` 并保持 `workflow_dispatch + release.published` 双触发，规避旧工作流元数据异常导致的触发失败。
- 修复：`release-assets-to-pages.yml` 中 `run` 脚本 heredoc 缩进导致 YAML 解析异常，改为 `printf` 生成 `index.html`，并将顶层 `on` 显式写为 `'on'` 保证触发器可识别。
- 决策：新增 `docs-dev/dev/xcdocs-workflow.md`，将 Apple API 变更统一为 `xcdocs search --json --omit-content -> xcdocs get --json` 的可追溯流程，并要求在功能文档验收标准中记录 `xcdocs` 证据入口。
- 文档：`docs-dev/features/simple-terminal.md` 验收标准新增一条，约束涉及 Apple Framework API 变更时必须附 `xcdocs` 检索证据并遵循工作流文档。
- 修复：无限画布卡片底部摘要越界；将非 compact 卡片的 `drawCardBodyHint` 绘制条件收敛为仅 `placeholder`，终端/文件夹卡片不再绘制底部长路径文案，并新增回归测试 `bodyHintShouldOnlyRenderForNonCompactPlaceholder`。
- 功能：在画布顶部新增“自动卡片尺寸”控件，提供 `320x220` 与 `480x320` 两档推荐尺寸；行为为“选中优先，无选中则应用全部卡片”，并补充模型层与 UI 交互回归测试。
- 修复：`Leucus` 的 Sparkle 更新器启动失败（弹窗 “Unable to Check For Updates”）；根因是产物缺少 `Contents/XPCServices`。
- 实现：`Example/Leucus.xcodeproj` 新增 Build Phase `Embed Sparkle XPC Services`，构建时复制 `Downloader.xpc` 与 `Installer.xpc` 到 `Leucus.app/Contents/XPCServices`。
- 版本：将 `Leucus` 版本提升到 `0.0.2 (2)`，用于测试更新链路。
- 验证：`xcodebuild -project Example/Leucus.xcodeproj -scheme Leucus -destination 'platform=macOS' build` 成功；产物已包含 `Contents/XPCServices/{Downloader,Installer}.xpc`，`Info.plist` 版本为 `CFBundleShortVersionString=0.0.2`、`CFBundleVersion=2`。
- 风险：`SUPublicEDKey` 仍为空，启动仅告警但后续签名校验可能失败；正式对外升级前需填入公钥并重新验证 appcast 签名。
- 发布：已创建 GitHub Release `v0.0.2`（`Leucus 0.0.2`），资产包含 `Leucus-0.0.2.zip` 与 `appcast.xml`。
- 同步：`release-assets-to-pages.yml` 在 `release` 事件下执行成功；`gh-pages` 分支 `appcast.xml` 已更新到 `0.0.2`。
- 修复：`Leucus` 在 Dock 重开事件链路崩溃，栈落在 `AppDelegate.createOrShowWindow()`；根因是 `main.swift` 将整个 `app.run()` 放在 `MainActor.assumeIsolated` 中，重入事件触发执行器假设断言。
- 实现：改为仅在初始化阶段（构建 `AppDelegate`、设置 `activationPolicy`、挂载 `delegate`）使用 `MainActor.assumeIsolated`，`app.run()` 移到外部，并用 `withExtendedLifetime(delegate)` 保持委托存活。
- 验证：`xcodebuild -project Example/Leucus.xcodeproj -scheme Leucus -configuration Debug -destination 'platform=macOS' build` 成功。
- 需求：文件夹卡片支持将文件/文件夹拖拽到另一个文件夹卡片完成归档。
- 实现：`FolderBrowserHostView` 新增拖拽源/目标能力（行可拖出，卡片可接收 `.fileURL` drop）；落盘行为通过 `FolderBrowserModel.moveEntries` 将拖入项移动到目标卡片当前目录，成功后即时刷新列表并回写目录变更回调。
- 验证：新增回归测试 `folderBrowserMoveEntriesShouldMoveFileAndFolderIntoTargetDirectory` 与 `folderBrowserMoveEntriesShouldIgnoreItemsAlreadyInTargetDirectory`，并通过定向执行。
- 体验：文件夹卡片新增拖拽悬停高亮（可接收时显示边框，拖出/放下后自动恢复）；补充测试 `dropHighlightStateShouldUpdateBorderStyle` 并通过。
- 调整：按反馈将“自动卡片尺寸”入口从画布顶部移除，改为每个非折叠卡片标题栏内“尺寸”按钮（两档：`320x220`、`480x320`），操作仅作用于当前卡片。
- 实现：`InfiniteCanvasView` 新增卡片头部尺寸按钮绘制、点击弹出菜单、按节点 ID 应用尺寸（`applyRecommendedCardSize(_:toNodeID:)`）；`CanvasWorkspaceView` 删除全局 `autoCardSizePopup`。
- 验证：新增回归测试 `workspaceShouldNotContainGlobalAutoCardSizeControl` 与 `canvasApplyCardSizeToNodeIDShouldOnlyAffectTargetNode`，并通过定向执行。
- 需求：支持通过组合键切换卡片选中状态，便于多选编排。
- 实现：`InfiniteCanvasView.mouseDown` 新增 `⌘/Shift + 点击` 分支；命中卡片后调用 `toggleNodeSelection(id:)`，实现“加入/移除当前卡片，不清空其他选择”。
- 验证：新增 `canvasToggleNodeSelectionShouldAddAndRemoveNode` 与 `commandClickShouldToggleNodeSelection` 并通过，原有 `canvasSelectNodeShouldBringTargetToFront` 回归通过。
- 需求：新增“设置…”页并支持自定义画布键位。
- 实现：`InfiniteCanvasKit` 新增 `CanvasInputBindings`（多选切换/平移/滚轮缩放修饰键）；`InfiniteCanvasView` 改为基于可配置键位判断交互；`Leucus` 菜单新增“设置…”（`⌘,`）窗口，支持三项键位配置，`UserDefaults` 持久化并实时应用到当前画布。
- 验证：新增 `canvasInputBindingsShouldMatchConfiguredModifiers` 单测通过；`xcodebuild -project Example/Leucus.xcodeproj -scheme Leucus -configuration Debug -destination 'platform=macOS' build` 通过。
- 调整：设置页布局参考本机 `Prowl`（`/Users/linhey/Desktop/临时需求/Prowl/supacode/Features/Settings/Views/SettingsView.swift`），改为左侧 sidebar（通用/键位）+ 右侧详情标题/副标题/内容，键位配置保留实时生效与持久化。
- 升级：引入 `KeyboardShortcuts`（`2.4.0`）作为键位录制库；在“设置 -> 键位”新增录制控件，支持自定义“自动整理卡片”“对齐卡片到网格”快捷键。
- 接线：`AppDelegate` 注册 `KeyboardShortcuts.onKeyUp` 触发画布动作；`ViewController` 暴露 `organizeCards` / `alignSelectionToGrid` 供快捷键回调调用。
- 验证：`xcodebuild -project Example/Leucus.xcodeproj -scheme Leucus -configuration Debug -destination 'platform=macOS' build` 通过；`swift test --filter InfiniteCanvasKitTests.canvasInputBindingsShouldMatchConfiguredModifiers` 通过。
- 体验升级：将“切换选中/平移/滚轮缩放”从下拉改为录制式 `ModifierRecorderField`（点击后按 `⌘/⇧/⌥/⌃`），样式与快捷键录制入口统一，配置即时生效并持久化。
- 调整：按反馈将设置页重构为 SwiftUI（`NavigationSplitView`）实现，形成更接近 Prowl 的“侧边栏 + 详情”结构；保留 `KeyboardShortcuts` 快捷键录制与修饰键录制（`NSViewRepresentable` 包装）。
- 修复：移除旧 AppKit 设置页导致的约束崩溃路径，设置窗口改用 `NSHostingController` 承载 SwiftUI 视图，避免跨层级 Auto Layout 约束异常。
- 验证：`xcodebuild -project Example/Leucus.xcodeproj -scheme Leucus -configuration Debug -destination 'platform=macOS' build` 通过。
- 体验增强：SwiftUI 设置页新增侧栏搜索、底部品牌区、详情渐变背景与分组卡片样式，视觉层次进一步贴近 Prowl。
- 验证：`xcodebuild -project Example/Leucus.xcodeproj -scheme Leucus -configuration Debug -destination 'platform=macOS' build` 通过（样式增强后）。
- 体验增强：设置页详情区新增顶部搜索框（与侧栏过滤联动）与“当前快捷配置”摘要条，便于快速核对录制结果。
- 验证：`xcodebuild -project Example/Leucus.xcodeproj -scheme Leucus -configuration Debug -destination 'platform=macOS' build` 通过（搜索栏与摘要条新增后）。
- 体验增强：设置页搜索支持空结果可视化，侧栏与详情区分别显示 empty state，避免“无反馈空白页”。
- 验证：`xcodebuild -project Example/Leucus.xcodeproj -scheme Leucus -configuration Debug -destination 'platform=macOS' build` 通过（empty state 新增后）。
- 调整：按反馈将“当前快捷配置”状态摘要从详情区迁移到侧边栏底部，信息层级与导航关系更一致。
- 验证：`xcodebuild -project Example/Leucus.xcodeproj -scheme Leucus -configuration Debug -destination 'platform=macOS' build` 通过（状态位置迁移后）。
- 修正：依据 SwiftUI 语义将状态摘要从 `.safeAreaInset` 迁移为侧边栏 `List` 内 section，确保“状态在侧边栏内”而非外层贴边。
- 验证：`xcodebuild -project Example/Leucus.xcodeproj -scheme Leucus -configuration Debug -destination 'platform=macOS' build` 通过（状态迁入 sidebar list 后）。
- 决策：针对“状态是否在侧边栏内”的争议，补跑 `$xcdocs`（`NavigationSplitView`、`ListStyle.sidebar`、`safeAreaInset`）后确认应将状态放入 `List` section，而非 `.safeAreaInset`。
- 排查：复现 Sparkle “Unable to Check For Updates” 后，通过 `log stream` 抓到两条关键错误：1) `XPC Service (Installer.xpc) must be in the Sparkle framework`（说明复制到 app 根目录是错误方案）；2) `The provided EdDSA key could not be decoded`（`SUPublicEDKey` 空值仍被视为无效 key）。
- 修复：回滚 `Embed Sparkle XPC Services` 方案，保持 Sparkle XPCServices 位于 `Sparkle.framework/Versions/B/XPCServices`；并从 `Example/Leucus/Info.plist` 删除 `SUPublicEDKey` 空占位，避免 updater 启动阶段 fatal。
- 验证：Release 构建实测（快捷键触发“检查更新…”）已从失败弹窗切换为 `You’re up to date!`，不再出现 `Unable to Check For Updates`。
- 发布：创建 GitHub Release `v0.0.4`（资产：`Leucus-0.0.4.zip`、`appcast.xml`），`release-assets-to-pages.yml` run `23376568961` 成功；`gh-pages` appcast 顶项已更新为 `0.0.4`。
- 风险：当前未配置 `SUPublicEDKey`，Sparkle 会给出“无 EdDSA key 已弃用”的告警；短期不阻塞升级链路，后续需补齐公私钥与签名流程。
- 规范化：已生成 Sparkle EdDSA 密钥（account: `linhay.Leucus`），公钥接入 `LEUCUS_SPARKLE_PUBLIC_KEY`，私钥导出到本机 `~/.config/leucus/sparkle_private_key.txt`（权限 `600`，未入库）。
- 防护：`LeucusUpdateConfiguration` 新增 `validatedPublicKey`（Base64 且解码后 32 字节）；`LeucusUpdater.startIfConfigured` 改为公钥无效即禁用更新器，避免 Sparkle fatal。
- 自动化：新增 `scripts/release_leucus_sparkle.sh` 与 `make release-leucus-sparkle VERSION=...`，统一执行 Release 构建、签名 appcast、上传 GitHub Release。
- 发布：已创建 `v0.0.5`（`Leucus 0.0.5`），并使用 EdDSA 私钥生成签名 `appcast.xml`；`release-assets-to-pages.yml` run `23376930242` 成功，`gh-pages` 顶项更新为 `0.0.5`。
- 验证：`0.0.5` 本地实测“检查更新…”正常（`You’re up to date!`），不再出现 `SUPublicEDKey` 解码 fatal。
- CI：新增 `.github/workflows/release-signed-sparkle.yml`，支持 Actions 内手动输入版本号触发“构建 + 签名 appcast + 创建/更新 Release”。
- 安全：已将私钥写入仓库 Secret `SPARKLE_PRIVATE_KEY`（`gh secret set`），发布签名可在 CI 侧完成，不依赖本机私钥参与。
- 决策：新增网页卡片能力采用“独立 target”路线，创建 `WebCard` 并由 `CanvasKit` 聚合导出（与 `TerminalCard`/`FolderCard` 一致）。
- 实现：新增 `WebCard` 模块（`SimpleWebCardOptions`/`SimpleWebCard`/`SimpleWebCardHostView`），并在 `InfiniteCanvasKit` 扩展 `CanvasNodeKind.web`、`CanvasNodeCard.web(...)`、`webURL` 字段与快照持久化字段。
- 交互：`InfiniteCanvasView` 右键菜单新增“添加网页卡片”，并支持 `terminal/folder/web` 互切；`CanvasWorkspaceView` 新增 WebCard 内容视图挂载、重排与独立窗口弹出回收链路。
- 文档：新增需求文档 `docs-dev/features/web-card.md`（包含 BDD 场景与验收标准）。
- 验证：`swift test` 全量执行时所有测试项均通过，但测试运行器收尾阶段触发 `signal 11`；定向验证 `swift test --filter WebCardTests`、`swift test --filter workspaceShouldRenderWebHostViewForWebNode`、`swift test --filter webFactoryShouldCreateWebKindNode` 均通过。
- 需求扩展：在 WebCard 基础上新增“编辑网页 URL + 最近网站”能力；流程为右键网页卡片可弹窗编辑地址，并可从“最近网站”子菜单一键应用。
- 实现：新增 `WebCardRecentSiteStore`（`UserDefaults` 持久化、URL 规范化、去重、最近优先、上限裁剪）；`InfiniteCanvasView` 接入“编辑网页地址…”与“最近网站”菜单，并在更新节点 URL 后回写最近列表。
- 实现：`SimpleWebCardHostView` 新增 `setURL(_:)` 与按 URL 去重加载逻辑，`CanvasWorkspaceView.syncWebCards` 在布局同步时可更新已有 WebView 的地址。
- 修复：统一 URL 解析规则，空 scheme（如 `://bad-url`）视为无效输入，避免落入最近网站列表。
- 验证：通过定向测试 `swift test --filter webRecentStore`、`swift test --filter WebCardTests`、`swift test --filter workspaceShouldRenderWebHostViewForWebNode`，并通过 `swift build` 编译验证。
- 需求：新增 Vapor 中枢服务，让卡片可通过统一命令通道互相控制。
- 决策：采用独立 target `CardHubService`（不与 UI 层耦合），对外提供 `POST /hub/commands`、`GET /hub/cards/:cardID/commands`、`GET /hub/health`。
- 实现：新增 `CardControlCommandRequest` / `CardControlCommand` / `CardCommandCenter` / `CardHubVapor` / `CardHubVaporService`；命令队列按 `targetCardID` 分发并在拉取时消费。
- 接线：`CanvasWorkspaceView` 新增 `attachCommandHub`/`detachCommandHub`/`applyHubCommand`，支持动作 `select`、`close`、`set-title`、`set-url`、`set-directory`。
- 兼容：`CanvasKit` 新增 `@_exported import CardHubService`，使用方可单独 `import CanvasKit` 获取中枢类型。
- 验证：新增 `CardHubServiceTests` 覆盖“投递-拉取-消费”与空 action 校验；新增 `workspaceShouldApplyHubSetTitleCommand`；定向测试与 `swift build` 通过。
- 接入：Example/Leucus 启动阶段自动初始化 `CardHubVaporService`（`127.0.0.1:28080`）并复用同一 `CardCommandCenter`，主画布 `ViewController` 创建后自动 `attachCommandHub`。
- 实现：`ViewController` 新增 `attachCommandHub(_:)` 与延迟 attach 逻辑（先设置 center，workspace 创建后自动绑定）；`AppDelegate` 在 `createOrShowWindow` 中注入 hub center，并在应用退出时停止 hub service。
- 验证：`xcodebuild -project Example/Leucus.xcodeproj -scheme Leucus -configuration Debug -destination 'platform=macOS' build` 通过；中枢与工作区链路相关定向测试通过。
- 扩展：中枢新增“按卡片路由控制”接口，支持 `POST /hub/cards/:cardID/select|close|title|url|directory`，并保持 `POST /hub/commands` 兼容。
- 实现：`CardHubVapor` 增加路由级请求模型 `CardControlRouteRequest` / `CardControlRouteValueRequest`，将路由请求映射为统一 `CardControlCommand` 入队。
- 校验：`title/url/directory` 路由增加空值拦截（400）。
- 验证：新增 `CardHubServiceTests` 路由映射与校验用例并通过（含动作顺序与 sourceCardID 断言）。
- 扩展：新增 `GET /hub/cards/:cardID/routes` 路由发现接口，返回每卡片可用控制动作描述（含 method/pathTemplate/映射 action/是否要求 value/scope）。
- 验证：新增中枢测试覆盖 routes 返回内容与非法 cardID 校验；`CardHubServiceTests` 全部通过。
- 调整：按约定将中枢路由前缀由 `hub` 统一切换为 `api`（包含 `/api/health`、`/api/commands`、`/api/cards/:cardID/...` 与 routes 发现接口）。
- 验证：`CardHubServiceTests` 全量通过，日志中已确认请求路径切换为 `/api/...`。
- 重构：CardHub 路由按主流 REST 重新设计为版本化资源模型，新增 canonical 路由前缀 `/api/v1`，核心结构为 `/api/v1/canvases/:canvasID/cards/:cardID/...`。
- 兼容：保留旧 `/api/...` 路由并映射到默认画布 `00000000-0000-0000-0000-000000000000`，避免现有调用链路中断。
- 模型：`CardControlCommand` 新增 `canvasID`；`CardCommandCenter` 队列键升级为 `(canvasID, cardID)`，实现多画布命令隔离。
- 路由：新增 `POST /api/v1/canvases/:canvasID/commands`、`GET /api/v1/canvases/:canvasID/cards/:cardID/commands`、`GET /api/v1/canvases/:canvasID/cards/:cardID/routes`、`POST /api/v1/canvases/:canvasID/cards/:cardID/actions/{select|close|title|url|directory}`。
- 测试：`CardHubServiceTests` 新增并通过多画布隔离用例 `commandsShouldBeIsolatedByCanvas`；现有路由映射与兼容路由测试同步通过。
- 文档：更新 `docs-dev/features/card-hub-vapor.md`（BDD 与新路由规范），新增 `docs-dev/api/openapi.yaml`（v1 API 契约）。
- 调整：按反馈将 canonical 资源命名从 `canvases/:canvasID` 统一切换为 `spaces/:spaceID`，语义对齐“哪个空间哪个卡片”。
- 兼容：保留 `CardHubService` 中 `defaultCanvasID`/`canvasID` 别名，以及 `/api/v1/canvases/...` 路由兼容映射，避免旧客户端立即断连。
- 路由：canonical 现为 `/api/v1/spaces/:spaceID/cards/:cardID/...`，兼容层包括 `/api/v1/canvases/...` 与旧 `/api/...`。
- 文档：同步更新 `docs-dev/features/card-hub-vapor.md` 与 `docs-dev/api/openapi.yaml` 到 `space` 命名。
- 验证：`swift test --filter CardHubServiceTests` 与 `swift test --filter workspaceShouldApplyHubSetTitleCommand` 通过。
- 决策：按反馈移除所有旧路由兼容层，不再保留 `/api/...` 与 `/api/v1/canvases/...`，只保留 `/api/v1/spaces/...`。
- 清理：移除 `CardControlCommand` 与 `CardRouteDiscoveryResponse` 的 `canvasID` 别名，移除 `CardHubVapor.defaultCanvasID`/`CardCommandCenter.defaultCanvasID` 别名，参数解析仅接受 `spaceID`。
- 测试：将旧兼容测试改为 `legacyRoutesShouldBeUnavailable`，断言旧路由返回 404；命名统一为 `commandsShouldBeIsolatedBySpace`。
- 验证：`swift test --filter CardHubServiceTests`、`swift test --filter workspaceShouldApplyHubSetTitleCommand` 通过。
