# 2026-03-20

- 决策：将节点拉伸命中从“点状把手”扩展为“边缘命中”，并保持“角优先于边”的判定顺序，避免角落歧义。
- 实现：新增 `CanvasNodeResizeGeometry` 统一处理命中几何与高亮边框映射；`InfiniteCanvasView` 接入 `mouseMoved` + tracking area，实现靠近边/角时高亮反馈。
- 验证：新增 3 个几何相关测试（角优先、边命中、高亮映射），`swift test` 全部通过（17 tests）；`xcodebuild -scheme CanvasTerminalKit` 构建成功。
- 决策：参考 Prowl 的 resize 流程，终端重排从“拖动中实时 reflow”改为“拖动期间冻结、松手后统一刷新一次”，减少 Ghostty 在缩放过程中的残影与错位。
- 决策：为 `SimpleTerminalHostView` 增加 AppKit 本地按键兜底链路（终端聚焦时拦截方向键/删除键并注入完整 ANSI 序列），规避方向键被当作裸文本 `[C/[D` 的问题。
- 验证：新增 `TerminalCardTests.appKitControlSequencesCoverArrowAndDeleteKeys`，覆盖 `0x7B/0x7C/0x7D/0x7E/0x33/0x75` 到 ANSI 序列映射；`swift test` 通过（17 tests）。
- 决策：继续在依赖层修复 `.exec` 输入链路（`libghostty-spm-main`）：`TerminalTextInputHandler@AppKit` 在 `.exec` 时不再处理方向键/删除键的 `doCommand` 文本回退；`TerminalKeyEventHandler@AppKit` 改为使用物理 `event.keyCode` 组包，和 Prowl 路径对齐。
- 决策：TerminalCard 终端 surface 切换为 Prowl 同源实现（`GhosttySurfaceView/GhosttyRuntime/GhosttySurfaceBridge`），并仅加最小兼容补丁以适配当前 Swift 6.2 + macOS 14 构建环境。
- 验证：`xcodebuild -project Example/CanvasTerminalExample.xcodeproj -scheme CanvasTerminalExample -destination 'platform=macOS' build` 通过并可启动 Example app。
- 风险：`swift test` 在本地 SwiftPM 运行器下出现 `Exited with unexpected signal code 11`（执行测试数为 0），属于工具链/运行器级问题，需后续单独排查。
- 修复：`GhosttyRuntime` 增加一次性 `ghostty_init` 初始化，解决 Example 启动时 `ghostty_config_new` 空指针崩溃（SIGSEGV）导致“窗口不显示”的问题。
- 当前验证：应用进程可稳定驻留（`.../CanvasTerminalExample.app/Contents/MacOS/CanvasTerminalExample`），`xcodebuild build` 成功，`swift test` 通过（17 tests，存在 `@preconcurrency` 警告）。
- 决策：终端卡片滚轮改为“终端优先、到边界透传画布”的路由策略；当终端无可滚动内容、命中边界方向或横向滚轮时，将事件透传给 InfiniteCanvas 以继续平移。
- 验证：新增 4 个滚轮路由测试（无滚动区、边界方向、中间区间、横向滚轮）；`swift test -q` 通过（21 tests），`xcodebuild ... build` 通过并可启动 Example。
- 修复：画布滚轮平移方向统一为双轴取反（`x/y` 同策略），解决终端卡片透传后体感方向反向的问题；并抽出 `CanvasScrollPanMapping` 供单测覆盖。
- 验证：新增 `scrollPanMappingShouldInvertBothAxes`，`swift test -q` 通过（22 tests），`xcodebuild ... build` 通过。
- 修正：按反馈调整滚轮映射为“水平同向、垂直反向”（`x = scrollDeltaX`, `y = -scrollDeltaY`），解决画布水平滚动方向反向问题。
- 验证：先改测试红灯再实现绿灯；`swift test -q` 通过（22 tests），`xcodebuild ... build` 通过。
- 决策：新增 `folder` 节点类型，并将“拖入文件夹”默认创建为文件夹卡片；终端卡片与文件夹卡片均归入 `CanvasNodeGroup.folder`。
- 实现：`InfiniteCanvasKit` 接入 `STFilePath`（`STFolder`）统一提取路径标题与大类标签；右键菜单新增“添加文件夹卡片”；Finder 打开能力扩展到 folder 节点。
- 验证：先补红灯测试（folder factory + finder path 覆盖），后实现转绿；`swift test -q` 通过（23 tests），`xcodebuild -project Example/CanvasTerminalExample.xcodeproj -scheme CanvasTerminalExample -destination 'platform=macOS' build` 通过。
- 修复：终端滚轮边界方向判定修正为“仅在当前方向无法继续滚动时透传画布”，避免终端仍可滚动时被画布抢占滚轮事件。
- 验证：先修改 `TerminalCardTests.terminalScrollRoutesToCanvasOnlyAtMatchingBoundary` 断言制造红灯，再调整 `GhosttyScrollWheelRouting` 实现转绿；`swift test` 全量通过（23 tests）。
- 决策：支持终端卡片与文件夹卡片互相切换，交互入口放在节点右键菜单，并按当前类型展示单一目标动作（`terminal -> folder` / `folder -> terminal`）。
- 实现：`CanvasNodeCard` 新增 `converted(to:)` 类型转换；`InfiniteCanvasView` 新增 `switchContextCardToTerminal/switchContextCardToFolder` 动作，保留节点 id/位置/尺寸/标题，仅变更类型与分组标签。
- 修复：补齐并统一 `bringNodeToFront` 单一实现，清理分支内重复定义导致的编译冲突。
- 验证：新增转换单测（terminal↔folder）并通过；`swift test -q` 通过（27 tests），`xcodebuild -project Example/CanvasTerminalExample.xcodeproj -scheme CanvasTerminalExample -destination 'platform=macOS' build` 通过。
- 修复：选中窗口时统一执行“选中+置顶”流程（`selectSingleNode`），将目标节点移动到 `nodes` 末尾，保证画布命中与绘制层级都以前景窗口优先。
- 修复：`CanvasWorkspaceView` 在同步终端卡片时按布局顺序重排 `terminalContainer` 子视图，确保终端内容层与画布卡片层级一致（选中窗口内容也在最前）。
- 验证：先新增 `bringNodeToFront` 红灯测试（命中节点置顶、缺失 id 保持顺序），后实现转绿；`swift test` 全量通过（27 tests）。
- 修复：终端内容区点击事件新增回调（`GhosttySurfaceView -> SimpleTerminalHostView -> CanvasWorkspaceView`），点击终端本体时也会调用 `canvasView.selectNode(id:)`，触发置顶。
- 修复：`InfiniteCanvasView` 新增公开 `selectNode(id:)`，并在内部选择逻辑增加“id 必须存在”保护，避免无效选择污染状态。
- 修复：`FolderBrowserModel.entries` 映射闭包写法修正，消除当前编译器下的闭包返回错误，恢复测试编译链路。
- 验证：新增 `canvasSelectNodeShouldBringTargetToFront` 测试；`swift test` 全量通过（31 tests）。
- 修复：缩放透传支持补齐。终端卡片在 `Command+滚轮` 时强制透传画布（不被终端滚动消费），并新增 `magnify` 透传回调；文件夹卡片同样支持 `Command+滚轮` 与 `magnify` 透传到画布。
- 验证：先补 `terminalCommandScrollAlwaysRoutesToCanvas` 红灯测试，再实现转绿；`swift test` 全量通过（32 tests）。
- 修复：终端滚轮边界透传策略升级为“仅在手势开始即边界时透传画布”；若同一滚动手势内是终端先滚动后触达边界，则不再链式透传到画布。
- 实现：`GhosttySurfaceView` 引入 `terminalConsumedInCurrentScrollGesture`，结合 `phase/momentumPhase` 管理手势生命周期，并将该状态注入 `shouldPassthroughToCanvas(...)` 判定。
- 验证：新增 `terminalScrollReachingBoundaryWithinSameGestureShouldNotRouteCanvas`；`swift test` 全量通过（33 tests）。
- 决策：文件夹卡片支持文件浏览，先提供最小可用交互（目录列表、双击进入子目录、Up 返回上级；双击文件交给系统打开）。
- 实现：新增 `InfiniteCanvasKit.FolderBrowserModel`（目录解析/父级路径/排序后的目录项）；`CanvasKit` 新增 `FolderBrowserHostView` 并接入 `CanvasWorkspaceView` 的内容层同步，`folder` 卡片渲染真实文件浏览视图。
- 验证：新增 3 个文件浏览模型测试（目录优先排序、根目录父级为空、非法路径回退）；`swift test -q` 通过（31 tests），`xcodebuild -project Example/CanvasTerminalExample.xcodeproj -scheme CanvasTerminalExample -destination 'platform=macOS' build` 通过。
- 修正：文件夹浏览内容层新增交互回调，点击浏览区即可激活并置顶对应卡片，避免内容层拦截后无法选中。
- 修复：移除文件夹浏览视图上的全局点击手势（会与 NSTableView 竞争），改为 `mouseDown/rightMouseDown + table action/selection` 驱动卡片激活，恢复目录项可点击与双击进入。
- 体验：文件夹浏览卡片改为深色风格，目录/文件使用系统符号图标，工具栏与路径文本按画布风格统一。
- 验证：`swift test -q` 通过（33 tests），`xcodebuild -project Example/CanvasTerminalExample.xcodeproj -scheme CanvasTerminalExample -destination 'platform=macOS' build` 通过。
- 架构：文件夹卡片已拆分为独立 `FolderCard` SPM target（与 `TerminalCard` 并列），`CanvasKit` 仅做编排层并依赖 `FolderCard`。
- 迁移：`FolderBrowserHostView` 从 `Sources/CanvasKit` 迁移到 `Sources/FolderCard`，并将可见 API 提升为 `public` 以供 `CanvasKit` 跨模块使用。
- 验证：`swift test -q` 通过（33 tests）；`xcodebuild -project Example/CanvasTerminalExample.xcodeproj -scheme CanvasTerminalExample -destination 'platform=macOS' build` 通过。
- 修复：文件夹卡片主题升级为强对比方案（`FolderBrowserTheme`）；列表区强制深色背景、亮色文字/图标、自定义选中高亮，避免出现“浅底浅字”不可读。
- 验证：先补 `FolderCardTests.browserUsesReadableHighContrastTheme` 红灯（背景/对比度断言失败），再改实现转绿；`swift test -q` 通过（34 tests），`xcodebuild -project Example/CanvasTerminalExample.xcodeproj -scheme CanvasTerminalExample -destination 'platform=macOS' build` 通过。
- 参考：对照 `ProwlCanvasTerminal/CanvasView` 的 organize/arrange 入口与网格策略，新增画布右键动作“自动整理卡片”“对齐卡片到网格”。
- 实现：`InfiniteCanvasKit` 新增 `organizeNodeCards(...)`（平衡列数 + 逐行按高度排布，避免重叠）与 `alignNodeCardsToGrid(...)`（按步长吸附，支持仅对齐选中节点；无选中则全量）。
- 测试：新增 2 个布局测试（整理后位置与无重叠、对齐仅作用目标节点）；先红灯（函数缺失）后绿灯，`swift test -q` 全量通过（36 tests）。
- 验证：`xcodebuild -project Example/CanvasTerminalExample.xcodeproj -scheme CanvasTerminalExample -destination 'platform=macOS' build` 通过。
- 调整：根据反馈将“对齐”入口从右键菜单移除，改为画布右下角悬浮按钮“对齐网格”；右键菜单仅保留“自动整理卡片”。
- 实现：`CanvasWorkspaceView` 新增右下角 `NSButton` 调用 `canvasView.alignSelectionToGrid()`；`InfiniteCanvasView` 新增公开方法 `alignSelectionToGrid` 与 `organizeCards` 供 UI 入口复用。
- 测试：新增 `canvasAlignSelectionToGridShouldSnapOnlySelectedNode`；`swift test -q` 全量通过（37 tests），`xcodebuild ... build` 通过。
- 修复：右下角“对齐网格”按钮在深色画布下可见性不足，新增 `CanvasAlignButtonStyle` 统一高对比样式（深色底 + 白字 + 边框 + 阴影），并在 `CanvasWorkspaceView` 统一调用。
- 测试：新增 `CanvasKitTests.alignButtonStyleShouldUseHighContrastPalette`，先红灯发现白字/亮蓝底对比度仅 `3.14`，后改为深色底转绿；`swift test -q` 全量通过（38 tests），`xcodebuild ... build` 通过。
- 需求：支持“卡片展开为独立窗口”，入口放在节点右键菜单（终端/文件夹卡片）。
- 实现：`InfiniteCanvasView` 新增 `onDetachNodeRequested` 回调与菜单动作“展开为独立窗口”；`CanvasWorkspaceView` 新增 detached window 管理（去重激活、窗口关闭回收、节点被删除时自动清理）。
- 实现：独立窗口内容按卡片类型渲染：terminal 使用 `SimpleTerminalHostView`（复用工作目录/标题），folder 使用 `FolderBrowserHostView`（目录变更回写节点并同步窗口标题）。
- 测试：先补 `InfiniteCanvasKitTests.nodeContextMenuShouldContainDetachActionAndEmitNodeID` 红灯（缺少回调属性），再实现转绿；`swift test --filter InfiniteCanvasKitTests -q` 通过（29 tests）。
- 风险：本地 SwiftPM 全量 `swift test -q` 仍偶发 `unexpected signal code 11`（执行前即崩）；已通过分套件与目标过滤测试以及 `xcodebuild` 构建验证本次改动正确性，问题属于既有测试运行器不稳定。
- 需求细化：独立窗口必须复用“展开前同一内容视图”，不能新建第二份终端/文件浏览实例；展开后画布卡片仅保留占位信息。
- 实现：`CanvasWorkspaceView` 新增 `detachedNodeIDs` 与视图迁移流程；展开时把 `SimpleTerminalHostView/FolderBrowserHostView` 从 `contentContainer` 移动到新窗口，关闭窗口再移动回画布并恢复回调。
- 行为：展开期间卡片内容不再在画布渲染（只留画布底层占位），重复点击“展开为独立窗口”会激活已有窗口；节点删除时自动关闭并清理对应独立窗口。
- 验证：`swift test --filter InfiniteCanvasKitTests.nodeContextMenuShouldContainDetachActionAndEmitNodeID -q` 通过，`swift test --filter InfiniteCanvasKitTests -q` 通过，`xcodebuild ... CanvasTerminalExample ... build` 通过。
- 修复：发现“展开为独立窗口”后内容视图被重排逻辑回流到画布，导致新窗口看不到内容；已在 `restackContentCards` 中跳过 `detachedNodeIDs`，并移除展开后立即 `syncCardContent` 的回流触发。
- 测试：新增 `CanvasKitTests.detachFolderCardShouldMoveSameHostViewToNewWindow`，验证展开后同一 `FolderBrowserHostView` 实例离开 workspace 子树并附着到新窗口。
- 验证：`swift test --filter CanvasKitTests.detachFolderCardShouldMoveSameHostViewToNewWindow -q` 通过；`xcodebuild -project Example/CanvasTerminalExample.xcodeproj -scheme CanvasTerminalExample -destination 'platform=macOS' build` 通过。
