# 2026-03-20

- 决策：将节点拉伸命中从“点状把手”扩展为“边缘命中”，并保持“角优先于边”的判定顺序，避免角落歧义。
- 实现：新增 `CanvasNodeResizeGeometry` 统一处理命中几何与高亮边框映射；`InfiniteCanvasView` 接入 `mouseMoved` + tracking area，实现靠近边/角时高亮反馈。
- 验证：新增 3 个几何相关测试（角优先、边命中、高亮映射），`swift test` 全部通过（17 tests）；`xcodebuild -scheme CanvasTerminalKit` 构建成功。
